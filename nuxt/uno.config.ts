// uno.config.ts
import { defineConfig } from "unocss";

export default defineConfig({
  shortcuts: {
    flexCenter: "flex items-center justify-center",
    flexVertically: "flex flex-col h-100",
    flexVerticallyCentered: "flex flex-col items-center h-100",
  },
});
